<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pigs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.exercise.ExercisesActivity"></activity>
        <activity android:name=".activities.workout.CreateWorkoutActivity"></activity>
        <activity android:name=".activities.exercise.CreateExerciseActivity"></activity>
        <activity android:name=".activities.exercise.CategoriesActivity"></activity>
        <activity android:name=".activities.workout.ScheduleActivity"></activity>
        <activity android:name=".activities.progress.ProgressActivity"></activity>
        <activity android:name=".activities.progress.GraphActivity"></activity>
        <activity android:name=".activities.authentication.LoginActivity"></activity>
        <activity android:name=".activities.authentication.RegistrationActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>